import ComingSoon from "@/components/ComingSoon";

export default function CalendarPage() {
  return (
    <ComingSoon
      title="Calendar"
      phase="Phase 4–5"
      desc="Availability windows synced from Google Calendar (Lisa's open blocks, checked against Lauren's schedule) and booked participant appointments."
    />
  );
}
